using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FoundationController : MonoBehaviour {

    //Present to show skeleton of work   
    //Use this script to detect prefab Cards 'card values', in order to count up from 0-13 (Ace - King). Once all four spaces contain 13, continue to end screen

	// Use this for initialization
	void Awake()
    {
		
	}
	
	// Update is called once per frame
	void Update ()
    {
		
	}
}
